#应用名称与概要必须复合唯一
#应用名称
spring.application.name=tempalte-name

#服务概要
spring.cloud.config.profile=${profile}

#配置中心url
spring.cloud.config.uri=${config.url}

#配置文件后缀 dev为测试环境配置对应application-dev proc为正式环境配置对应application-proc
spring.profiles.active=${run.profile}
